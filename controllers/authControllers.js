import userSchema from "../models/userModel.js";

const registerControllers = async (req, res)=>{
    try {
        const {fame, email, phone, password, address} = req.body;
    } catch (error) {
        
    }
}